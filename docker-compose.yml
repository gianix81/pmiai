services:
  app:
    build: .
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      # persiste il database SQLite fuori dal container
      - ./data:/app/data
